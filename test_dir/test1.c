#include <stdio.h>
#include <string.h>

int main () {
    char str1[201], str2[201];
    scanf("%s", str1);
    printf("%d", 'a' - 'A');

//    for (int i = 0, l = (int) strlen(str1); i < l; ++i) {
//        if ('A' <= str1[i] && str1[i] <= 'Z') str2[i] = str1[i] + 'a' - 'A';
//
//    }
}
